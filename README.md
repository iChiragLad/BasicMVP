Pretty self-explainatory -- provides a reasonable MVP/MVC pattern within the WPF setup (App.xaml usage, etc), so that someone can churn out a GUI with minimal effort and maximum readability/OOP usage.
